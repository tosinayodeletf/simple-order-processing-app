pipeline {
agent any
stages {
      stage('Checkout code') {
           steps { 
              git branch: 'main',
                  url: 'https://github.com/tosinayodeletf/simple-order-processing-app.git'
     }
   }
       stage('Build') {
            steps {
                echo 'Running minimal build step'
                sh 'ls -l'
               }
        }
    }
}
